文件上传

[拖拽API](https://developer.mozilla.org/zh-CN/docs/Web/API/HTML_Drag_and_Drop_API)`ondragenter` `ondrop`

后端在卸载外部程序集时，一定要执行一次GC否则卸载上下文后，需要等待GC被动回收，此时应用程序持有文件，因此外部在上传文件，后端删除时，会无法删除。

因此在存储外部程序集对象时，一定要明确存放位置，否则无法真正回收，文件占用也无法释放。
